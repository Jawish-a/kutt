services:
  server:
    build:
      context: .
    volumes:
       - db_data_sqlite:/var/lib/lysa
       - custom:/lysa/custom
    environment:
      DB_FILENAME: "/var/lib/lysa/data.sqlite"
    ports:
      - 3000:3000
volumes:
  db_data_sqlite:
  custom:
